apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nc-learners
  namespace: default
spec:
  selector:
    matchLabels:
      app.kubernetes.io/part-of: nc-learners
  endpoints:
    - interval: 10s
      port: metrics-port
      scrapeTimeout: 9s
  namespaceSelector:
    matchNames:
      - default
  